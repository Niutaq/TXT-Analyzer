# TXT-Analyzer
A small functional-style tool that processes text streams and outputs key statistics. It loads a text file (e.g., a book or server logs) and returns metrics such as most frequent words, sentence count, average word length, and detection of “banned words.”
